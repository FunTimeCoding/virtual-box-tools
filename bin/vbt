#!/usr/bin/env python3
import sys

from virtual_box_tools.host_config_main import HostConfigMain

if __name__ == '__main__':
    application = HostConfigMain(sys.argv[1:])
    exit_code = application.run()
    sys.exit(exit_code)
